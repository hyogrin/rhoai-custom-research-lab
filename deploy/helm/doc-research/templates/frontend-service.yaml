apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    {{- include "doc-research.componentLabels" (dict "name" "frontend" "root" .) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "doc-research.selectorLabels" (dict "name" "frontend" "root" .) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}
      protocol: TCP
