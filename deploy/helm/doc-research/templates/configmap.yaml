apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "doc-research.configMapName" . }}
  labels:
    {{- include "doc-research.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
